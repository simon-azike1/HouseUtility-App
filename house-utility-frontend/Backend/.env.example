# Backend Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit .env with secrets to version control

PORT=5000
NODE_ENV=development

# MongoDB Connection (Atlas or local)
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Secret (use a strong random string, minimum 32 chars)
JWT_SECRET=your-secret-key-here-minimum-32-characters-long
JWT_EXPIRE=7d

# Frontend URLs (comma-separated for multiple dev servers)
# Example: http://localhost:3000,http://localhost:3002
FRONTEND_URLS=http://localhost:3000

# Google OAuth Configuration
# Get these from https://console.cloud.google.com
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Production: Update these for your deployment domain
# FRONTEND_URLS=https://yourdomain.com
# GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback
# NODE_ENV=production

# Feedback owner notifications
OWNER_EMAIL=owner@example.com
# WhatsApp number in E.164 format, e.g. +15551234567
OWNER_WHATSAPP=+15551234567
FEEDBACK_WHATSAPP_ENABLED=false

# Admin access (email-based)
ADMIN_EMAIL=.....

# SendGrid (recommended for production)
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_FROM_EMAIL=verified_sender@yourdomain.com

# Contact form recipient
CONTACT_EMAIL=owner@example.com

# Optional token fallback for browsers that block third-party cookies
ENABLE_TOKEN_FALLBACK=false
